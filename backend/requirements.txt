fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic-settings==2.7.1
# Japanese morphological analysis (word-level readings). fugashi wheels bundle
# MeCab; unidic-lite bundles the dictionary — no apt/system packages needed.
fugashi==1.5.2
unidic-lite==1.0.8
# Offline dictionaries (JMdict words + KanjiDic2 kanji), fully bundled — the
# backend needs no database or network for lookups.
jamdict==0.1a11.post2
jamdict-data==1.5
# Build Anki decks (.apkg) from the glossary.
genanki==0.13.1
# ytmusicapi is installed from the vendored fork (see Dockerfile).
